# CNN classifier model

This folder has my CNN version for the digit challenge.

Train:

```bash
python -m src.iivp_project.train_simple
```

Make Kaggle CSV:

```bash
python -m src.iivp_project.predict_simple
```
